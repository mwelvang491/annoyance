@model IEnumerable<NorthwindClass.Models.Product>

@{
    ViewBag.Title = "product";
}

<h2>Browse Products</h2>
<p>Select from @Model.Count() products:</p>

<ul>
    @foreach (var p in Model)
    {
        <li>@p.productName - @String.Format("{0:c}", @p.productPrice)</li>
    }
</ul>